package com.booking.KBookin.repository.projection.wishlist;

public interface WishlistPropertiesProjection {

    Long getId();           // WishlistProperty.id

    Long getWishlistId();   // Wishlist.id

    Long getPropertyId();   // Property.id
}
